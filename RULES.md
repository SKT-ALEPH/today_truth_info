# T04 RULES — 오늘의 진짜 정보판

정본은 제공된 `criterion-registry.json`의 T04-C01~T04-C35다. 각 조건은 개별적으로 확인하며, 확인할 수 없는 조건은 완료로 추정하지 않는다.

| RULE | 구현 및 확인 위치 |
| --- | --- |
| T04-C01 | 결과물·소스 URL을 계정 생성 없이 시크릿 창에서 확인한다. |
| T04-C02 | 결과물·소스 URL을 로그인 없이 시크릿 창에서 확인한다. |
| T04-C03 | `/api/live`가 Open-Meteo 공개 동적 값을 조회한다. |
| T04-C04 | 실제 정보 카드에 정규화 값을 표시한다. |
| T04-C05 | 실제 정보 카드와 기록표에 단위를 표시한다. |
| T04-C06 | 출처 패널에 Open-Meteo 이름과 URL을 표시한다. |
| T04-C07 | 출처 패널에 `source_time`을 RFC 3339로 표시한다. |
| T04-C08 | 출처 패널에 `fetched_at`을 KST RFC 3339로 표시한다. |
| T04-C09 | 상단과 출처 패널에 `Asia/Seoul`을 표시한다. |
| T04-C10 | 원자료·저장값·화면값 대조 패널에서 한 건의 숫자 일치를 확인한다. |
| T04-C11 | DB 연결 문자열은 서버 전용 `DATABASE_URL`에만 두고 보안 검색을 실행한다. |
| T04-C12 | T04-TIMEOUT을 `stale / timeout`으로 재생한다. |
| T04-C13 | T04-AUTH-401을 `stale / auth`로 재생한다. |
| T04-C14 | T04-RATE-429를 `stale / rate_limit`으로 재생한다. |
| T04-C15 | T04-OFFLINE을 `stale / offline`으로 재생한다. |
| T04-C16 | T04-SCHEMA-BREAK를 `stale / schema_error`로 재생한다. |
| T04-C17 | 모든 실패에서 마지막 정상값 105와 기존 행을 유지한다. |
| T04-C18 | 모든 실패에서 마지막 정상값에 `stale`을 표시한다. |
| T04-C19 | 다시 시도 시 T04-RECOVER-D2로 `fresh / none`, 2행, 값 120을 만든다. |
| T04-C20 | D1-A와 D1-B가 같은 record ID와 한 행을 유지한다. |
| T04-C21 | D2 성공 시 다음 날짜의 두 번째 행을 만든다. |
| T04-C22 | 실제 KST 날짜가 다른 두 DB 행만 보존하고 둘째 행 이후 봉인한다. |
| T04-C23 | 두 행의 출처 URL·시각·값·단위를 원자료, DB, 공개 화면에서 대조한다. |
| T04-C24 | 두 DB 값으로 signed day-over-day delta를 재계산해 식과 결과를 표시한다. |
| T04-C25 | 사용자 입력·위치·계정·분석기를 두지 않고 제출 파일에 개인정보를 넣지 않는다. |
| T04-C26 | 장애 재생은 `/public/vendor/.../fixtures`의 합성 값만 사용한다. |
| T04-C27 | `SUBMISSION.md`에 위치·3단계 행동·통과 모습·실패 모습을 구분한다. |
| T04-C28 | `SUBMISSION.md`에 AI 역할·직접 판단·따르지 않은 제안을 구분한다. |
| T04-C29 | 결과물·소스 URL을 별도 인증 없이 시크릿 창에서 확인한다. |
| T04-C30 | 결과물·소스 URL을 초대 없이 시크릿 창에서 확인한다. |
| T04-C31 | 결과물·소스 URL을 비밀번호 없이 시크릿 창에서 확인한다. |
| T04-C32 | 결과물·소스 URL을 OAuth 연결 없이 시크릿 창에서 확인한다. |
| T04-C33 | 결과물·소스 URL을 CAPTCHA 없이 시크릿 창에서 확인한다. |
| T04-C34 | 최종 제출의 결과물 필드에 공개 HTTPS URL 하나를 넣는다. |
| T04-C35 | 최종 제출의 소스 필드에 40자리 소문자 full commit이 포함된 공개 HTTPS URL 하나를 넣는다. |

## 완료 잠금

- `tests/contract.test.ts`가 35개 정본 ID와 공개 asset 해시를 검사한다.
- `tests/domain.test.ts`가 정상·동일 날짜·다음 날짜·오류·회복 상태 전이를 검사한다.
- `scripts/scan-secrets.mjs`가 소스와 생성 결과에서 비밀값 형태를 검색한다.
- 실제 이틀과 공개 URL은 배포 뒤에만 확인할 수 있으므로 그 전에는 C01~C03, C10~C11, C22~C25, C29~C35를 완료로 표시하지 않는다.
